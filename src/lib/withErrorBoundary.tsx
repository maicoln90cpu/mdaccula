/**
 * Re-export central do HOC `withErrorBoundary` e do `SectionErrorBoundary`.
 * Facilita imports a partir de `@/lib/withErrorBoundary` em vez de
 * `@/components/ErrorBoundary`.
 */
export { withErrorBoundary, SectionErrorBoundary, ErrorBoundary } from "@/components/ErrorBoundary";
