/** Fonte única de assunto, preheader e placeholders para frontend e Edge Functions. */
export type { EmailMeta, EmailMetaPlaceholderData } from "@shared/emailMeta.ts";
export { buildEmailMeta, injectEmailPreheader, resolveEmailPlaceholders } from "@shared/emailMeta.ts";
