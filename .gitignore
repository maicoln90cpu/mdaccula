# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
playwright-report/
test-results/
blob-report/
.playwright/

# Vitest coverage (gerado; baseline ratchet é versionada)
coverage/

# Opção B — Fallback local de deploy de Edge Functions (bundle-edge-functions.mjs)
# Artefatos gerados; NUNCA commitar. O deploy oficial (Opção C / GitHub Actions)
# usa o index.ts original, que resolve _shared/ pelo CLI da Supabase.
supabase/functions/*/index.bundled.ts
supabase/functions/*/index.original.ts
